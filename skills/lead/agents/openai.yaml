interface:
  display_name: "Agent Team Lead"
  short_description: "Coordinate a project team in Codex or Orca"
  default_prompt: "Use $lead to initialize or coordinate the current project's agent team."
policy:
  allow_implicit_invocation: true
